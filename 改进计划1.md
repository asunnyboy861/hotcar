# 📋 HotCar 应用优化改进计划（修订版）

> **文档版本**: v1.1  
> **创建日期**: 2026-03-10  
> **修订日期**: 2026-03-10  
> **目标**: 系统性优化 HotCar 应用，提升美国市场用户体验和代码质量

---

## ⚠️ 重要修订说明

本文档已根据现有代码库进行全面修订，确保：
1. ✅ **复用现有模块** - 不重复造轮子
2. ✅ **保持架构一致** - 遵循现有设计模式
3. ✅ **兼容现有代码** - 不破坏已有功能
4. ✅ **最小化改动** - 只修改必要的部分

---

## 📑 目录

1. [现有代码分析](#现有代码分析)
2. [优化原则](#优化原则)
3. [优化内容概览](#优化内容概览)
4. [高优先级优化项](#高优先级优化项)
5. [中优先级优化项](#中优先级优化项)
6. [低优先级优化项](#低优先级优化项)
7. [实施时间表](#实施时间表)
8. [验证标准](#验证标准)

---

## 🔍 现有代码分析

### ✅ 已存在的完整模块

以下模块已经完整实现，**无需重新创建**：

| 模块名称 | 文件路径 | 功能状态 | 备注 |
|---------|---------|---------|------|
| LocationService | Features/Location/LocationService.swift | ✅ 完整 | 已有缓存功能 |
| HapticFeedbackService | Features/Timer/HapticFeedbackService.swift | ✅ 完整 | 完整的触觉反馈实现 |
| NotificationService | Features/Timer/NotificationService.swift | ✅ 完整 | 完整的通知实现 |
| WidgetDataService | Features/Settings/WidgetDataService.swift | ✅ 完整 | Widget 数据共享 |
| AppSettings | Features/Settings/AppSettings.swift | ✅ 完整 | 缺少地区检测 |
| SettingsService | Features/Settings/AppSettings.swift | ✅ 完整 | 设置管理 |
| WarmUpSession | Features/Statistics/WarmUpSession.swift | ✅ 完整 | 会话数据模型 |
| StatisticsService | Features/Statistics/StatisticsService.swift | ✅ 完整 | 统计服务 |
| VehicleService | Features/VehicleManagement/VehicleService.swift | ✅ 完整 | 车辆管理 |
| CountdownTimer | Features/Timer/CountdownTimer.swift | ⚠️ 部分 | 需要增强 |

### ⚠️ 需要增强的模块

以下模块已存在但需要增强：

| 模块名称 | 当前状态 | 需要增强的功能 |
|---------|---------|---------------|
| CountdownTimer | 基础功能完整 | 状态持久化、Widget 集成、会话保存 |
| AppSettings | 功能完整 | 地区检测、默认华氏度 |
| WeatherService | 功能完整 | 集成 LocationService |
| HomeViewModel | 功能完整 | 集成 CountdownTimer |

### ❌ 需要新增的模块

以下模块需要新增：

| 模块名称 | 用途 | 优先级 |
|---------|------|--------|
| TimerState | 计时器状态模型 | 高 |
| LiveActivityManager | Live Activity 管理 | 中 |
| Intents | Siri 快捷指令 | 中 |
| ShareService | 社交分享 | 中 |

---

## 🎯 优化原则

### 1. 功能模块原则
- ✅ **单一职责**: 每个模块只负责一个功能
- ✅ **高内聚低耦合**: 模块内部紧密关联，模块之间松散耦合
- ✅ **命名清晰**: 使用语义化命名，见名知意
- ✅ **文件结构语义化**: 目录结构清晰反映功能划分

### 2. 代码复用原则
- ✅ **优先复用现有模块**: 不重复造轮子
- ✅ **三次法则**: 遇到三次重复代码再抽象
- ✅ **最小代码原则**: 能合并的尽量合并

### 3. 重构清理原则
- ✅ **标记废弃**: 使用 `@available(*, deprecated)` 标记
- ✅ **验证无影响**: 确保无引用后再删除
- ✅ **提交信息清晰**: 注明清理范围和原因

### 4. 风格一致性原则
- ✅ **UI 风格统一**: 遵循现有设计系统
- ✅ **代码风格一致**: 遵循项目编码规范
- ✅ **功能逻辑连贯**: 与现有功能无缝衔接

### 5. 继承兼容原则
- ✅ **优先继承**: 利用现有类和协议
- ✅ **避免硬编码**: 使用配置和继承
- ✅ **向后兼容**: 不破坏现有功能

---

## 📊 优化内容概览

| 优先级 | 优化项 | 预计工作量 | 影响范围 | 代码复用情况 |
|--------|--------|-----------|---------|-------------|
| 🔴 高 | 集成 LocationService 到 WeatherService | 1小时 | WeatherService | 复用 LocationService |
| 🔴 高 | 增强 CountdownTimer | 2小时 | CountdownTimer, HomeViewModel | 复用现有服务 |
| 🔴 高 | 添加地区检测到 AppSettings | 1小时 | AppSettings | 复用 SettingsService |
| 🟡 中 | Live Activity 支持 | 4小时 | 新增 LiveActivity 模块 | 复用 CountdownTimer |
| 🟡 中 | Siri 快捷指令 | 3小时 | 新增 Intents 模块 | 复用 VehicleService |
| 🟡 中 | 社交分享功能 | 2小时 | StatisticsView | 复用 StatisticsService |
| 🟢 低 | AI 学习功能 | 6小时 | 新增 ML 模块 | 新增模块 |
| 🟢 低 | 多语言支持 | 4小时 | 新增 Localization 模块 | 新增模块 |

---

## 🔴 高优先级优化项

### 优化项 1: 集成 LocationService 到 WeatherService

#### 📝 需求描述
当前 WeatherService 中硬编码了多伦多坐标，需要集成已存在的 LocationService 获取用户真实位置。

#### 🔍 现有代码分析

**已有模块**:
- ✅ `LocationService.swift` - 已存在，有完整功能包括缓存
- ✅ `WeatherService.swift` - 已存在，需要修改

**需要修改**:
- WeatherService 使用 LocationService 获取位置
- 移除硬编码坐标

#### 🎯 生成规则

##### 1. 文件结构
```
Features/
├── Location/
│   └── LocationService.swift (已存在，无需修改)
├── WeatherIntegration/
│   └── WeatherService.swift (需修改)
```

##### 2. 代码编写示例

**WeatherService.swift 修改**
```swift
//
//  WeatherService.swift
//  hotcar
//
//  HotCar Service - Weather Integration
//  Modified to use LocationService
//

import Foundation

final class WeatherService: ObservableObject {
    
    // MARK: - Singleton
    
    static let shared = WeatherService()
    
    // MARK: - Published Properties
    
    @Published var currentTemperature: Double?
    @Published var tomorrowMorningTemp: Double?
    @Published var isLoading: Bool = false
    @Published var errorMessage: String?
    
    // MARK: - Dependencies (复用现有服务)
    
    private let baseURL = "https://api.open-meteo.com/v1/forecast"
    private let locationService = LocationService.shared // 复用现有服务
    
    // MARK: - Initialization
    
    private init() {}
    
    // MARK: - Public Methods
    
    @MainActor
    func fetchCurrentTemperature() async {
        isLoading = true
        errorMessage = nil
        
        // Get location from LocationService (复用现有服务)
        let location = locationService.getCurrentLocation()
        
        let urlString = "\(baseURL)?latitude=\(location.latitude)&longitude=\(location.longitude)&current_weather=true&temperature_unit=celsius"
        
        guard let url = URL(string: urlString) else {
            errorMessage = "Invalid URL"
            isLoading = false
            return
        }
        
        do {
            let (data, response) = try await URLSession.shared.data(from: url)
            
            guard let httpResponse = response as? HTTPURLResponse,
                  (200...299).contains(httpResponse.statusCode) else {
                errorMessage = "Server error"
                isLoading = false
                return
            }
            
            let decoder = JSONDecoder()
            let weatherData = try decoder.decode(WeatherResponse.self, from: data)
            
            await MainActor.run {
                self.currentTemperature = weatherData.currentWeather.temperature
                self.isLoading = false
            }
        } catch {
            await MainActor.run {
                self.errorMessage = error.localizedDescription
                self.isLoading = false
            }
        }
    }
    
    @MainActor
    func fetchTomorrowMorningTemperature() async {
        let location = locationService.getCurrentLocation()
        
        let urlString = "\(baseURL)?latitude=\(location.latitude)&longitude=\(location.longitude)&hourly=temperature_2m&forecast_days=2&temperature_unit=celsius"
        
        guard let url = URL(string: urlString) else {
            return
        }
        
        do {
            let (data, response) = try await URLSession.shared.data(from: url)
            
            guard let httpResponse = response as? HTTPURLResponse,
                  (200...299).contains(httpResponse.statusCode) else {
                return
            }
            
            let decoder = JSONDecoder()
            let weatherData = try decoder.decode(HourlyWeatherResponse.self, from: data)
            
            // Get temperature for 7 AM tomorrow
            let tomorrow7AM = Calendar.current.startOfDay(for: Date())
                .addingTimeInterval(24 * 60 * 60 + 7 * 60 * 60)
            
            if let index = weatherData.hourly.time.firstIndex(where: { $0 >= tomorrow7AM }) {
                await MainActor.run {
                    self.tomorrowMorningTemp = weatherData.hourly.temperature[index]
                }
            }
        } catch {
            print("Error fetching tomorrow's temperature: \(error)")
        }
    }
    
    /// Refresh weather data with fresh location
    @MainActor
    func refreshWeatherWithLocation() async {
        // Force refresh location (复用现有方法)
        locationService.forceRefreshLocation()
        
        // Wait a bit for location update
        try? await Task.sleep(nanoseconds: 1_000_000_000) // 1 second
        
        // Fetch weather
        await fetchCurrentTemperature()
        await fetchTomorrowMorningTemperature()
    }
}

// MARK: - Weather Response Models (保持不变)

struct WeatherResponse: Codable {
    let currentWeather: CurrentWeather
    
    enum CodingKeys: String, CodingKey {
        case currentWeather = "current_weather"
    }
}

struct CurrentWeather: Codable {
    let temperature: Double
}

struct HourlyWeatherResponse: Codable {
    let hourly: HourlyData
}

struct HourlyData: Codable {
    let time: [Date]
    let temperature: [Double]
    
    enum CodingKeys: String, CodingKey {
        case time
        case temperature = "temperature_2m"
    }
}
```

##### 3. 测试用例

**WeatherServiceTests.swift**
```swift
//
//  WeatherServiceTests.swift
//  hotcarTests
//

import XCTest
@testable import hotcar

final class WeatherServiceTests: XCTestCase {
    
    var sut: WeatherService!
    
    override func setUp() {
        super.setUp()
        sut = WeatherService.shared
    }
    
    override func tearDown() {
        sut = nil
        super.tearDown()
    }
    
    // MARK: - URL Construction Tests
    
    func testWeatherURLConstruction() {
        // Given
        let latitude = 40.7128
        let longitude = -74.0060
        
        // When
        let urlString = "https://api.open-meteo.com/v1/forecast?latitude=\(latitude)&longitude=\(longitude)&current_weather=true&temperature_unit=celsius"
        
        // Then
        XCTAssertTrue(urlString.contains("latitude=40.7128"))
        XCTAssertTrue(urlString.contains("longitude=-74.0060"))
        XCTAssertTrue(urlString.contains("temperature_unit=celsius"))
    }
    
    // MARK: - JSON Decoding Tests
    
    func testWeatherResponseDecoding() throws {
        // Given
        let json = """
        {
            "current_weather": {
                "temperature": -5.2
            }
        }
        """.data(using: .utf8)!
        
        // When
        let decoder = JSONDecoder()
        let response = try decoder.decode(WeatherResponse.self, from: json)
        
        // Then
        XCTAssertEqual(response.currentWeather.temperature, -5.2)
    }
}
```

#### ✅ 验证标准

##### 功能验证
- [ ] 天气数据使用真实位置
- [ ] 位置缓存正常工作
- [ ] 位置权限被拒绝时使用默认位置

##### 代码复用验证
- [ ] 复用 LocationService.shared
- [ ] 复用 getCurrentLocation() 方法
- [ ] 复用 forceRefreshLocation() 方法

---

### 优化项 2: 增强 CountdownTimer

#### 📝 需求描述
当前 CountdownTimer 功能基础完整，需要添加状态持久化、Widget 集成和会话保存功能。

#### 🔍 现有代码分析

**已有模块**:
- ✅ `CountdownTimer.swift` - 已存在，有基础功能
- ✅ `HapticFeedbackService.swift` - 已存在，完整实现
- ✅ `NotificationService.swift` - 已存在，完整实现
- ✅ `WidgetDataService.swift` - 已存在，完整实现
- ✅ `StatisticsService.swift` - 已存在，完整实现
- ✅ `WarmUpSession.swift` - 已存在，完整实现

**需要增强**:
- 添加状态持久化
- 集成 WidgetDataService
- 集成 StatisticsService 保存会话

#### 🎯 生成规则

##### 1. 文件结构
```
Features/
├── Timer/
│   ├── CountdownTimer.swift (已存在，需增强)
│   ├── TimerState.swift (新增)
│   ├── HapticFeedbackService.swift (已存在，无需修改)
│   └── NotificationService.swift (已存在，无需修改)
├── Statistics/
│   └── WarmUpSession.swift (已存在，复用)
├── Settings/
│   └── WidgetDataService.swift (已存在，复用)
```

##### 2. 代码编写示例

**TimerState.swift 新增**
```swift
//
//  TimerState.swift
//  hotcar
//
//  HotCar Timer - Timer State Model
//  Represents the state of a warm-up timer
//

import Foundation

/// Timer state enumeration
enum TimerState: Equatable {
    case idle
    case running(startTime: Date, duration: TimeInterval)
    case paused(remaining: TimeInterval)
    case completed
    
    var isActive: Bool {
        switch self {
        case .running, .paused:
            return true
        default:
            return false
        }
    }
    
    var isRunning: Bool {
        if case .running = self { return true }
        return false
    }
    
    var isPaused: Bool {
        if case .paused = self { return true }
        return false
    }
}

/// Timer display data
struct TimerDisplayData {
    let minutes: Int
    let seconds: Int
    let progress: Double
    let state: TimerState
    
    var formattedTime: String {
        String(format: "%02d:%02d", minutes, seconds)
    }
    
    var progressPercentage: Int {
        Int(progress * 100)
    }
}

/// Timer persisted state for recovery
struct TimerPersistedState: Codable {
    let state: TimerState
    let timeRemaining: Int
    let totalTime: Int
    let vehicleId: String?
    let vehicleName: String?
    let temperature: Double?
    let startTime: Date?
}

// MARK: - TimerState Codable

extension TimerState: Codable {
    enum CodingKeys: String, CodingKey {
        case type
        case startTime
        case duration
        case remaining
    }
    
    init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        let type = try container.decode(String.self, forKey: .type)
        
        switch type {
        case "idle":
            self = .idle
        case "running":
            let startTime = try container.decode(Date.self, forKey: .startTime)
            let duration = try container.decode(TimeInterval.self, forKey: .duration)
            self = .running(startTime: startTime, duration: duration)
        case "paused":
            let remaining = try container.decode(TimeInterval.self, forKey: .remaining)
            self = .paused(remaining: remaining)
        case "completed":
            self = .completed
        default:
            self = .idle
        }
    }
    
    func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        
        switch self {
        case .idle:
            try container.encode("idle", forKey: .type)
        case .running(let startTime, let duration):
            try container.encode("running", forKey: .type)
            try container.encode(startTime, forKey: .startTime)
            try container.encode(duration, forKey: .duration)
        case .paused(let remaining):
            try container.encode("paused", forKey: .type)
            try container.encode(remaining, forKey: .remaining)
        case .completed:
            try container.encode("completed", forKey: .type)
        }
    }
}
```

**CountdownTimer.swift 增强**
```swift
//
//  CountdownTimer.swift
//  hotcar
//
//  HotCar Timer - Countdown Timer with Progress
//  Enhanced with state management and persistence
//

import Foundation
import Combine

final class CountdownTimer: ObservableObject {
    
    // MARK: - Published Properties
    
    @Published var timeRemaining: Int // in seconds
    @Published var isRunning: Bool = false
    @Published var isPaused: Bool = false
    @Published var totalTime: Int = 0
    @Published var state: TimerState = .idle
    
    // MARK: - Computed Properties
    
    var minutes: Int {
        timeRemaining / 60
    }
    
    var seconds: Int {
        timeRemaining % 60
    }
    
    var progress: Double {
        guard totalTime > 0 else { return 0 }
        return Double(totalTime - timeRemaining) / Double(totalTime)
    }
    
    var isComplete: Bool {
        timeRemaining <= 0 && !isRunning
    }
    
    var displayData: TimerDisplayData {
        TimerDisplayData(
            minutes: minutes,
            seconds: seconds,
            progress: progress,
            state: state
        )
    }
    
    // MARK: - Dependencies (复用现有服务)
    
    private var timer: Timer?
    private var cancellables = Set<AnyCancellable>()
    private let hapticService = HapticFeedbackService.shared // 已存在
    private let notificationService = NotificationService.shared // 已存在
    private let widgetService = WidgetDataService.shared // 已存在
    private let statisticsService = StatisticsService.shared // 已存在
    
    // Session tracking
    private var currentSessionStartTime: Date?
    private var currentVehicleId: String?
    private var vehicleName: String = "Vehicle"
    private var currentTemperature: Double = 0
    
    // MARK: - Initialization
    
    init(totalMinutes: Int = 0) {
        self.totalTime = totalMinutes * 60
        self.timeRemaining = totalMinutes * 60
        loadPersistedState()
    }
    
    deinit {
        timer?.invalidate()
    }
    
    // MARK: - Public Methods
    
    func start(minutes: Int, vehicleName: String = "Vehicle", vehicleId: String = "", temperature: Double = 0) {
        stop()
        
        self.vehicleName = vehicleName
        self.currentVehicleId = vehicleId
        self.currentTemperature = temperature
        totalTime = minutes * 60
        timeRemaining = totalTime
        isRunning = true
        isPaused = false
        state = .running(startTime: Date(), duration: TimeInterval(totalTime))
        
        // Track session data
        currentSessionStartTime = Date()
        
        // Haptic feedback on start (复用现有服务)
        hapticService.timerStart()
        
        // Schedule notification (复用现有服务)
        Task {
            await notificationService.scheduleTimerCompleteNotification(
                minutes: minutes,
                vehicleName: vehicleName
            )
        }
        
        // Update widget (复用现有服务)
        widgetService.updateTimerState(
            isRunning: true,
            remaining: timeRemaining,
            vehicleName: vehicleName
        )
        
        timer = Timer.scheduledTimer(withTimeInterval: 1.0, repeats: true) { [weak self] _ in
            self?.tick()
        }
        
        // Persist state
        persistState()
    }
    
    func pause() {
        guard isRunning && !isPaused else { return }
        
        isPaused = true
        state = .paused(remaining: TimeInterval(timeRemaining))
        timer?.invalidate()
        
        hapticService.timerTick()
        
        // Update widget
        widgetService.updateTimerState(
            isRunning: false,
            remaining: timeRemaining,
            vehicleName: vehicleName
        )
        
        persistState()
    }
    
    func resume() {
        guard isPaused else { return }
        
        isPaused = false
        state = .running(startTime: Date(), duration: TimeInterval(timeRemaining))
        
        hapticService.timerStart()
        
        timer = Timer.scheduledTimer(withTimeInterval: 1.0, repeats: true) { [weak self] _ in
            self?.tick()
        }
        
        // Update widget
        widgetService.updateTimerState(
            isRunning: true,
            remaining: timeRemaining,
            vehicleName: vehicleName
        )
        
        persistState()
    }
    
    func stop() {
        isRunning = false
        isPaused = false
        state = .idle
        timer?.invalidate()
        timer = nil
        
        // Cancel notifications (复用现有服务)
        Task {
            await notificationService.cancelAllTimerNotifications()
        }
        
        // Update widget
        widgetService.updateTimerState(
            isRunning: false,
            remaining: 0,
            vehicleName: ""
        )
        
        // Clear session data
        currentSessionStartTime = nil
        currentVehicleId = nil
        
        // Clear persisted state
        clearPersistedState()
    }
    
    func reset(minutes: Int) {
        stop()
        totalTime = minutes * 60
        timeRemaining = totalTime
        state = .idle
    }
    
    // MARK: - Private Methods
    
    private func tick() {
        guard timeRemaining > 0 else {
            complete()
            return
        }
        
        timeRemaining -= 1
        
        // Update widget every 10 seconds
        if timeRemaining % 10 == 0 {
            widgetService.updateTimerState(
                isRunning: true,
                remaining: timeRemaining,
                vehicleName: vehicleName
            )
        }
        
        // Haptic feedback every minute (复用现有服务)
        if timeRemaining > 0 && timeRemaining % 60 == 0 {
            hapticService.timerTick()
        }
        
        // Persist state every 30 seconds
        if timeRemaining % 30 == 0 {
            persistState()
        }
    }
    
    private func complete() {
        stop()
        state = .completed
        
        // Create and save session (复用现有 WarmUpSession)
        if let startTime = currentSessionStartTime,
           let vehicleId = currentVehicleId {
            
            let session = WarmUpSession(
                id: UUID().uuidString,
                vehicleId: vehicleId,
                vehicleName: vehicleName,
                startTime: startTime,
                endTime: Date(),
                durationMinutes: totalTime / 60,
                outsideTemperature: currentTemperature,
                targetTemperature: nil,
                vehicleType: "sedan", // TODO: Get from vehicle
                engineType: "gasoline", // TODO: Get from vehicle
                completed: true,
                notes: nil
            )
            
            Task {
                await statisticsService.addSession(session)
            }
        }
        
        // Strong haptic feedback (复用现有服务)
        hapticService.timerComplete()
    }
    
    // MARK: - State Persistence
    
    private let stateKey = "com.hotcar.timer.state"
    
    private func persistState() {
        let stateData = TimerPersistedState(
            state: state,
            timeRemaining: timeRemaining,
            totalTime: totalTime,
            vehicleId: currentVehicleId,
            vehicleName: vehicleName,
            temperature: currentTemperature,
            startTime: currentSessionStartTime
        )
        
        if let data = try? JSONEncoder().encode(stateData) {
            UserDefaults.standard.set(data, forKey: stateKey)
        }
    }
    
    private func loadPersistedState() {
        guard let data = UserDefaults.standard.data(forKey: stateKey),
              let stateData = try? JSONDecoder().decode(TimerPersistedState.self, from: data) else {
            return
        }
        
        // Only restore if timer was running and hasn't expired
        if case .running(let startTime, _) = stateData.state {
            let elapsed = Date().timeIntervalSince(startTime)
            let remaining = stateData.totalTime - Int(elapsed)
            
            if remaining > 0 {
                // Restore timer
                timeRemaining = remaining
                totalTime = stateData.totalTime
                state = .running(startTime: startTime, duration: TimeInterval(stateData.totalTime))
                currentVehicleId = stateData.vehicleId
                vehicleName = stateData.vehicleName ?? "Vehicle"
                currentTemperature = stateData.temperature ?? 0
                currentSessionStartTime = stateData.startTime
                
                // Resume timer
                timer = Timer.scheduledTimer(withTimeInterval: 1.0, repeats: true) { [weak self] _ in
                    self?.tick()
                }
            } else {
                // Timer expired while app was closed
                complete()
            }
        }
    }
    
    private func clearPersistedState() {
        UserDefaults.standard.removeObject(forKey: stateKey)
    }
}

// MARK: - Haptic Feedback

extension CountdownTimer {
    
    func triggerHapticFeedback() {
        hapticService.timerTick()
    }
}
```

##### 3. 测试用例

**CountdownTimerTests.swift**
```swift
//
//  CountdownTimerTests.swift
//  hotcarTests
//

import XCTest
@testable import hotcar

final class CountdownTimerTests: XCTestCase {
    
    var sut: CountdownTimer!
    
    override func setUp() {
        super.setUp()
        sut = CountdownTimer()
    }
    
    override func tearDown() {
        sut.stop()
        sut = nil
        super.tearDown()
    }
    
    // MARK: - Timer State Tests
    
    func testInitialState() {
        // Then
        XCTAssertFalse(sut.state.isActive)
        XCTAssertEqual(sut.timeRemaining, 0)
        XCTAssertEqual(sut.totalTime, 0)
    }
    
    func testStartTimer() {
        // When
        sut.start(minutes: 10, vehicleName: "Test Car")
        
        // Then
        XCTAssertTrue(sut.state.isActive)
        XCTAssertEqual(sut.timeRemaining, 600) // 10 minutes in seconds
        XCTAssertEqual(sut.totalTime, 600)
    }
    
    func testPauseTimer() {
        // Given
        sut.start(minutes: 10)
        
        // When
        sut.pause()
        
        // Then
        XCTAssertTrue(sut.state.isPaused)
    }
    
    func testResumeTimer() {
        // Given
        sut.start(minutes: 10)
        sut.pause()
        
        // When
        sut.resume()
        
        // Then
        XCTAssertTrue(sut.state.isRunning)
    }
    
    func testStopTimer() {
        // Given
        sut.start(minutes: 10)
        
        // When
        sut.stop()
        
        // Then
        XCTAssertFalse(sut.state.isActive)
        XCTAssertEqual(sut.timeRemaining, 0)
    }
    
    // MARK: - Progress Calculation Tests
    
    func testProgressCalculation() {
        // Given
        sut.start(minutes: 10)
        sut.timeRemaining = 300 // 5 minutes remaining
        
        // When
        let progress = sut.progress
        
        // Then
        XCTAssertEqual(progress, 0.5, accuracy: 0.01) // 50% complete
    }
    
    // MARK: - Display Data Tests
    
    func testDisplayDataFormatting() {
        // Given
        sut.start(minutes: 5)
        sut.timeRemaining = 185 // 3:05
        
        // When
        let displayData = sut.displayData
        
        // Then
        XCTAssertEqual(displayData.minutes, 3)
        XCTAssertEqual(displayData.seconds, 5)
        XCTAssertEqual(displayData.formattedTime, "03:05")
    }
}
```

#### ✅ 验证标准

##### 功能验证
- [ ] 计时器启动后正常倒计时
- [ ] 暂停/恢复功能正常
- [ ] 计时完成后触发通知
- [ ] 触觉反馈正常工作
- [ ] Widget 数据同步更新
- [ ] 应用关闭后状态恢复
- [ ] 会话数据正确保存到 WarmUpSession

##### 代码复用验证
- [ ] 复用 HapticFeedbackService.shared
- [ ] 复用 NotificationService.shared
- [ ] 复用 WidgetDataService.shared
- [ ] 复用 StatisticsService.shared
- [ ] 复用 WarmUpSession

---

### 优化项 3: 添加地区检测到 AppSettings

#### 📝 需求描述
针对美国市场，默认使用华氏度作为温度单位，并提供智能地区检测。

#### 🔍 现有代码分析

**已有模块**:
- ✅ `AppSettings.swift` - 已存在，有温度单位枚举
- ✅ `SettingsService.swift` - 已存在，有保存功能

**需要增强**:
- 添加地区检测初始化
- 添加温度格式化方法

#### 🎯 生成规则

##### 1. 代码编写示例

**AppSettings.swift 修改**
```swift
//
//  AppSettings.swift
//  hotcar
//
//  HotCar Settings - App Settings Model
//  Enhanced with region-based defaults
//

import Foundation

struct AppSettings: Codable {
    // Temperature
    var temperatureUnit: TemperatureUnit
    var defaultTargetTemp: Double = 20.0
    
    // Timer
    var defaultTimerDuration: Int = 15
    var autoStartTimer: Bool = false
    var showNotifications: Bool = true
    var hapticFeedback: Bool = true
    
    // Vehicle
    var primaryVehicleId: String?
    var autoDetectLocation: Bool = true
    
    // Appearance
    var darkMode: DarkModeSetting = .system
    
    // Privacy
    var analyticsEnabled: Bool = true
    var crashReportingEnabled: Bool = true
    
    // MARK: - Initialization with Region Detection
    
    init() {
        // Detect user's region and set default temperature unit
        let locale = Locale.current
        let regionCode = locale.region?.identifier ?? "US"
        
        // Use Fahrenheit for US, Liberia, Myanmar; Celsius for others
        let fahrenheitRegions = ["US", "LR", "MM"]
        self.temperatureUnit = fahrenheitRegions.contains(regionCode) ? .fahrenheit : .celsius
    }
    
    // MARK: - Nested Types (保持不变)
    
    enum TemperatureUnit: String, Codable, CaseIterable {
        case celsius = "celsius"
        case fahrenheit = "fahrenheit"
        
        var displayName: String {
            switch self {
            case .celsius: return "Celsius (°C)"
            case .fahrenheit: return "Fahrenheit (°F)"
            }
        }
        
        var symbol: String {
            switch self {
            case .celsius: return "°C"
            case .fahrenheit: return "°F"
            }
        }
        
        /// Convert from Celsius to current unit
        func convert(from celsius: Double) -> Double {
            switch self {
            case .celsius:
                return celsius
            case .fahrenheit:
                return (celsius * 9/5) + 32
            }
        }
        
        /// Convert from current unit to Celsius
        func convertToCelsius(_ value: Double) -> Double {
            switch self {
            case .celsius:
                return value
            case .fahrenheit:
                return (value - 32) * 5/9
            }
        }
        
        /// Format temperature with unit symbol
        func format(_ celsius: Double) -> String {
            let value = convert(from: celsius)
            return String(format: "%.0f%@", value, symbol)
        }
    }
    
    enum DarkModeSetting: String, Codable, CaseIterable {
        case light = "light"
        case dark = "dark"
        case system = "system"
        
        var displayName: String {
            switch self {
            case .light: return "Light"
            case .dark: return "Dark"
            case .system: return "System"
            }
        }
    }
}

// MARK: - Settings Service (保持不变，无需修改)

@MainActor
final class SettingsService: ObservableObject {
    
    // MARK: - Singleton
    
    static let shared = SettingsService()
    
    // MARK: - Published Properties
    
    @Published var settings: AppSettings
    
    // MARK: - Dependencies
    
    private let storageKey = "com.hotcar.settings"
    
    // MARK: - Initialization
    
    private init() {
        // Try to load saved settings
        if let data = UserDefaults.standard.data(forKey: storageKey),
           let decoded = try? JSONDecoder().decode(AppSettings.self, from: data) {
            settings = decoded
        } else {
            // Create new settings with region detection
            settings = AppSettings()
        }
    }
    
    // MARK: - Public Methods (保持不变)
    
    func loadSettings() async {
        // Already loaded in init
    }
    
    func saveSettings() {
        if let encoded = try? JSONEncoder().encode(settings) {
            UserDefaults.standard.set(encoded, forKey: storageKey)
        }
    }
    
    func updateTemperatureUnit(_ unit: AppSettings.TemperatureUnit) {
        settings.temperatureUnit = unit
        saveSettings()
        
        // Post notification for temperature display updates
        NotificationCenter.default.post(
            name: .temperatureUnitDidChange,
            object: nil,
            userInfo: ["unit": unit]
        )
    }
    
    func updateDefaultTimerDuration(_ minutes: Int) {
        settings.defaultTimerDuration = minutes
        saveSettings()
    }
    
    func updateAutoStartTimer(_ enabled: Bool) {
        settings.autoStartTimer = enabled
        saveSettings()
    }
    
    func updateNotifications(_ enabled: Bool) {
        settings.showNotifications = enabled
        saveSettings()
    }
    
    func updateHapticFeedback(_ enabled: Bool) {
        settings.hapticFeedback = enabled
        saveSettings()
    }
    
    func updateDarkMode(_ mode: AppSettings.DarkModeSetting) {
        settings.darkMode = mode
        saveSettings()
    }
    
    func updatePrimaryVehicle(_ vehicleId: String?) {
        settings.primaryVehicleId = vehicleId
        saveSettings()
    }
    
    func resetToDefaults() {
        settings = AppSettings()
        saveSettings()
    }
}

// MARK: - Notification Names

extension Notification.Name {
    static let temperatureUnitDidChange = Notification.Name("temperatureUnitDidChange")
}
```

##### 2. 测试用例

**AppSettingsTests.swift**
```swift
//
//  AppSettingsTests.swift
//  hotcarTests
//

import XCTest
@testable import hotcar

final class AppSettingsTests: XCTestCase {
    
    // MARK: - Region Detection Tests
    
    func testUSRegionDefaultsToFahrenheit() {
        // Given
        let locale = Locale(identifier: "en_US")
        
        // When
        let regionCode = locale.region?.identifier ?? "US"
        let fahrenheitRegions = ["US", "LR", "MM"]
        let expectedUnit: AppSettings.TemperatureUnit = fahrenheitRegions.contains(regionCode) ? .fahrenheit : .celsius
        
        // Then
        XCTAssertEqual(expectedUnit, .fahrenheit)
    }
    
    func testCanadaRegionDefaultsToCelsius() {
        // Given
        let locale = Locale(identifier: "en_CA")
        
        // When
        let regionCode = locale.region?.identifier ?? "CA"
        let fahrenheitRegions = ["US", "LR", "MM"]
        let expectedUnit: AppSettings.TemperatureUnit = fahrenheitRegions.contains(regionCode) ? .fahrenheit : .celsius
        
        // Then
        XCTAssertEqual(expectedUnit, .celsius)
    }
    
    // MARK: - Temperature Conversion Tests
    
    func testCelsiusToFahrenheit() {
        // Given
        let unit: AppSettings.TemperatureUnit = .fahrenheit
        let celsius = 0.0
        
        // When
        let fahrenheit = unit.convert(from: celsius)
        
        // Then
        XCTAssertEqual(fahrenheit, 32.0, accuracy: 0.01)
    }
    
    func testFahrenheitToCelsius() {
        // Given
        let unit: AppSettings.TemperatureUnit = .fahrenheit
        let fahrenheit = 32.0
        
        // When
        let celsius = unit.convertToCelsius(fahrenheit)
        
        // Then
        XCTAssertEqual(celsius, 0.0, accuracy: 0.01)
    }
    
    func testNegativeTemperatureConversion() {
        // Given
        let unit: AppSettings.TemperatureUnit = .fahrenheit
        let celsius = -40.0
        
        // When
        let fahrenheit = unit.convert(from: celsius)
        
        // Then
        XCTAssertEqual(fahrenheit, -40.0, accuracy: 0.01) // -40°C = -40°F
    }
    
    func testTemperatureFormatting() {
        // Given
        let unit: AppSettings.TemperatureUnit = .fahrenheit
        let celsius = -25.0
        
        // When
        let formatted = unit.format(celsius)
        
        // Then
        XCTAssertEqual(formatted, "-13°F")
    }
}
```

#### ✅ 验证标准

##### 功能验证
- [ ] 美国地区默认华氏度
- [ ] 加拿大地区默认摄氏度
- [ ] 设置更改后立即生效
- [ ] 设置持久化保存
- [ ] 温度显示自动更新

##### 代码复用验证
- [ ] 复用 SettingsService.shared
- [ ] 复用 AppSettings 结构
- [ ] 复用 TemperatureUnit 枚举

---

## 🟡 中优先级优化项

### 优化项 4: Live Activity 支持

#### 📝 需求描述
为 iPhone 14 Pro 及以上设备添加 Dynamic Island 和锁屏 Live Activity 支持，实时显示计时器进度。

#### 🔍 现有代码分析

**已有模块**:
- ✅ `TimerState` - 新增，可复用
- ✅ `CountdownTimer` - 已增强
- ✅ `WidgetDataService` - 已存在，可复用

**需要新增**:
- Live Activity 模块
- ActivityKit 集成

#### 🎯 生成规则

##### 1. 文件结构
```
hotcarWidget/
├── LiveActivity/
│   ├── WarmUpLiveActivity.swift (新增)
│   ├── WarmUpLiveActivityAttributes.swift (新增)
│   └── WarmUpLiveActivityView.swift (新增)
```

##### 2. 代码编写示例

**WarmUpLiveActivityAttributes.swift**
```swift
//
//  WarmUpLiveActivityAttributes.swift
//  hotcarWidget
//
//  HotCar Live Activity - Attributes
//  Defines the data structure for Live Activity
//

import ActivityKit
import Foundation

/// Warm-up timer Live Activity attributes
struct WarmUpActivityAttributes: ActivityAttributes {
    public struct ContentState: Codable, Hashable {
        /// Remaining time in seconds
        var remainingTime: Int
        
        /// Total time in seconds
        var totalTime: Int
        
        /// Vehicle name
        var vehicleName: String
        
        /// Current temperature (Celsius)
        var temperature: Double
        
        /// Timer state
        var isPaused: Bool
        
        /// Progress percentage (0.0 to 1.0)
        var progress: Double {
            guard totalTime > 0 else { return 0 }
            return Double(totalTime - remainingTime) / Double(totalTime)
        }
        
        /// Formatted remaining time
        var formattedTime: String {
            let minutes = remainingTime / 60
            let seconds = remainingTime % 60
            return String(format: "%02d:%02d", minutes, seconds)
        }
    }
    
    /// Vehicle ID for reference
    var vehicleId: String
    
    /// Warm-up start time
    var startTime: Date
}
```

**WarmUpLiveActivity.swift**
```swift
//
//  WarmUpLiveActivity.swift
//  hotcarWidget
//
//  HotCar Live Activity - Implementation
//  Provides Dynamic Island and Lock Screen live updates
//

import ActivityKit
import WidgetKit
import SwiftUI

@available(iOS 16.1, *)
struct WarmUpLiveActivity: Widget {
    
    var body: some WidgetConfiguration {
        ActivityConfiguration(for: WarmUpActivityAttributes.self) { context in
            // Lock screen / banner UI
            LockScreenLiveActivityView(context: context)
        } dynamicIsland: { context in
            // Dynamic Island UI
            DynamicIsland {
                // Expanded view
                DynamicIslandExpandedRegion(.leading) {
                    HStack {
                        Image(systemName: "car.fill")
                            .foregroundColor(.hotCarPrimary)
                        Text(context.state.vehicleName)
                            .font(.caption)
                            .foregroundColor(.textSecondary)
                    }
                }
                
                DynamicIslandExpandedRegion(.trailing) {
                    Text(context.state.formattedTime)
                        .font(.caption)
                        .monospacedDigit()
                        .foregroundColor(.textPrimary)
                }
                
                DynamicIslandExpandedRegion(.bottom) {
                    // Progress bar
                    VStack(spacing: 8) {
                        ProgressView(value: context.state.progress)
                            .tint(.hotCarPrimary)
                        
                        Text("\(Int(context.state.progress * 100))% complete")
                            .font(.caption2)
                            .foregroundColor(.textSecondary)
                    }
                    .padding()
                }
            } compactLeading: {
                // Compact leading (left side of island)
                Image(systemName: "flame.fill")
                    .foregroundColor(.hotCarSecondary)
            } compactTrailing: {
                // Compact trailing (right side of island)
                Text(context.state.formattedTime)
                    .font(.caption)
                    .monospacedDigit()
                    .foregroundColor(.textPrimary)
            } minimal: {
                // Minimal view (when multiple activities)
                Image(systemName: "timer")
                    .foregroundColor(.hotCarPrimary)
            }
        }
    }
}

// MARK: - Lock Screen View

@available(iOS 16.1, *)
struct LockScreenLiveActivityView: View {
    let context: ActivityViewContext<WarmUpActivityAttributes>
    
    var body: some View {
        VStack(spacing: 12) {
            // Header
            HStack {
                Image(systemName: "car.fill")
                    .foregroundColor(.hotCarPrimary)
                
                Text(context.state.vehicleName)
                    .font(.headline)
                    .foregroundColor(.textPrimary)
                
                Spacer()
                
                Text(context.state.formattedTime)
                    .font(.title2)
                    .fontWeight(.bold)
                    .foregroundColor(.textPrimary)
            }
            
            // Progress bar
            GeometryReader { geometry in
                ZStack(alignment: .leading) {
                    RoundedRectangle(cornerRadius: 4)
                        .fill(Color.textMuted.opacity(0.3))
                    
                    RoundedRectangle(cornerRadius: 4)
                        .fill(
                            LinearGradient(
                                colors: [.hotCarPrimary, .hotCarSecondary],
                                startPoint: .leading,
                                endPoint: .trailing
                            )
                        )
                        .frame(width: geometry.size.width * context.state.progress)
                }
            }
            .frame(height: 8)
            
            // Status
            HStack {
                Text(context.state.isPaused ? "Paused" : "Warming up...")
                    .font(.caption)
                    .foregroundColor(.textSecondary)
                
                Spacer()
                
                Text("\(Int(context.state.progress * 100))%")
                    .font(.caption)
                    .foregroundColor(.textSecondary)
            }
        }
        .padding()
        .background(Color.backgroundSecondary)
        .cornerRadius(16)
    }
}

// MARK: - Live Activity Manager

@available(iOS 16.1, *)
class LiveActivityManager {
    
    static let shared = LiveActivityManager()
    
    private var currentActivity: Activity<WarmUpActivityAttributes>?
    
    private init() {}
    
    /// Start a new Live Activity
    func startActivity(
        vehicleId: String,
        vehicleName: String,
        duration: Int,
        temperature: Double
    ) {
        // Check if Live Activity is available
        guard ActivityAuthorizationInfo().areActivitiesEnabled else {
            print("Live Activities are not enabled")
            return
        }
        
        // End any existing activity
        endActivity()
        
        // Create attributes
        let attributes = WarmUpActivityAttributes(
            vehicleId: vehicleId,
            startTime: Date()
        )
        
        // Create initial state
        let initialState = WarmUpActivityAttributes.ContentState(
            remainingTime: duration,
            totalTime: duration,
            vehicleName: vehicleName,
            temperature: temperature,
            isPaused: false
        )
        
        // Start activity
        do {
            currentActivity = try Activity<WarmUpActivityAttributes>.request(
                attributes: attributes,
                content: .init(state: initialState, staleDate: nil),
                pushType: nil
            )
            
            print("Live Activity started: \(currentActivity?.id ?? "unknown")")
        } catch {
            print("Failed to start Live Activity: \(error)")
        }
    }
    
    /// Update the Live Activity state
    func updateActivity(
        remainingTime: Int,
        isPaused: Bool
    ) {
        guard let activity = currentActivity else { return }
        
        let updatedState = WarmUpActivityAttributes.ContentState(
            remainingTime: remainingTime,
            totalTime: activity.content.state.totalTime,
            vehicleName: activity.content.state.vehicleName,
            temperature: activity.content.state.temperature,
            isPaused: isPaused
        )
        
        Task {
            await activity.update(using: .init(state: updatedState, staleDate: nil))
        }
    }
    
    /// End the Live Activity
    func endActivity() {
        guard let activity = currentActivity else { return }
        
        Task {
            await activity.end(using: nil, dismissalPolicy: .default)
            currentActivity = nil
        }
    }
    
    /// End the Live Activity with completion message
    func endActivityWithCompletion() {
        guard let activity = currentActivity else { return }
        
        let finalState = WarmUpActivityAttributes.ContentState(
            remainingTime: 0,
            totalTime: activity.content.state.totalTime,
            vehicleName: activity.content.state.vehicleName,
            temperature: activity.content.state.temperature,
            isPaused: false
        )
        
        Task {
            await activity.end(
                using: .init(
                    state: finalState,
                    staleDate: nil
                ),
                dismissalPolicy: .default
            )
            currentActivity = nil
        }
    }
}
```

##### 3. 集成到 CountdownTimer

在 `CountdownTimer.swift` 中添加 Live Activity 支持：

```swift
// Add to CountdownTimer class

// MARK: - Live Activity Support

@available(iOS 16.1, *)
private var liveActivityManager: LiveActivityManager {
    LiveActivityManager.shared
}

private func startLiveActivity(vehicleId: String, vehicleName: String, temperature: Double) {
    if #available(iOS 16.1, *) {
        liveActivityManager.startActivity(
            vehicleId: vehicleId,
            vehicleName: vehicleName,
            duration: totalTime,
            temperature: temperature
        )
    }
}

private func updateLiveActivity() {
    if #available(iOS 16.1, *) {
        liveActivityManager.updateActivity(
            remainingTime: timeRemaining,
            isPaused: state.isPaused
        )
    }
}

private func endLiveActivity() {
    if #available(iOS 16.1, *) {
        liveActivityManager.endActivity()
    }
}

private func endLiveActivityWithCompletion() {
    if #available(iOS 16.1, *) {
        liveActivityManager.endActivityWithCompletion()
    }
}
```

#### ✅ 验证标准

##### 功能验证
- [ ] Live Activity 正常启动
- [ ] Dynamic Island 显示正确
- [ ] 锁屏界面显示正确
- [ ] 进度实时更新
- [ ] 暂停/恢复状态同步
- [ ] 计时完成自动结束

##### 兼容性验证
- [ ] iPhone 14 Pro+ Dynamic Island 正常
- [ ] iPhone 14 及以下锁屏 Live Activity 正常
- [ ] iOS 16.1+ 正常运行
- [ ] iOS 16.0 及以下优雅降级

---

### 优化项 5: Siri 快捷指令

#### 📝 需求描述
添加 Siri 快捷指令支持，允许用户通过语音命令启动预热计时器。

#### 🔍 现有代码分析

**已有模块**:
- ✅ `VehicleService` - 已存在，可复用
- ✅ `WeatherService` - 已增强
- ✅ `CountdownTimer` - 已增强
- ✅ `SettingsService` - 已增强

**需要新增**:
- Intents 模块
- AppIntents 集成

#### 🎯 生成规则

##### 1. 文件结构
```
Intents/
├── StartWarmUpIntent.swift (新增)
├── CheckTemperatureIntent.swift (新增)
└── VehicleEntity.swift (新增)
```

##### 2. 代码编写示例

**VehicleEntity.swift**
```swift
//
//  VehicleEntity.swift
//  hotcarIntents
//
//  HotCar Siri Intent - Vehicle Entity
//

import AppIntents
import Foundation

@available(iOS 16.0, *)
struct VehicleEntity: AppEntity {
    
    static var typeDisplayRepresentation = TypeDisplayRepresentation(name: "Vehicle")
    static var defaultQuery = VehicleQuery()
    
    var id: String
    var displayString: String
    var displayRepresentation: DisplayRepresentation {
        DisplayRepresentation(title: "\(displayString)")
    }
}

// MARK: - Vehicle Query

@available(iOS 16.0, *)
struct VehicleQuery: EntityQuery {
    
    func entities(for identifiers: [String]) async throws -> [VehicleEntity] {
        let vehicles = VehicleService.shared.vehicles
        return vehicles
            .filter { identifiers.contains($0.id) }
            .map { VehicleEntity(id: $0.id, displayString: $0.name) }
    }
    
    func suggestedEntities() async throws -> [VehicleEntity] {
        let vehicles = VehicleService.shared.vehicles
        return vehicles.map { VehicleEntity(id: $0.id, displayString: $0.name) }
    }
    
    func defaultResult() async -> VehicleEntity? {
        let vehicles = VehicleService.shared.vehicles
        guard let primary = vehicles.first(where: { $0.isPrimary }) ?? vehicles.first else {
            return nil
        }
        return VehicleEntity(id: primary.id, displayString: primary.name)
    }
}
```

**StartWarmUpIntent.swift**
```swift
//
//  StartWarmUpIntent.swift
//  hotcarIntents
//
//  HotCar Siri Intent - Start Warm-Up Timer
//

import AppIntents
import Foundation

@available(iOS 16.0, *)
struct StartWarmUpIntent: AppIntent {
    
    static var title: LocalizedStringResource = "Start Warm-Up Timer"
    static var description = IntentDescription(
        "Start the vehicle warm-up timer based on current temperature"
    )
    
    static var openAppWhenRun: Bool = false
    
    @Parameter(title: "Vehicle", description: "The vehicle to warm up")
    var vehicle: VehicleEntity?
    
    @MainActor
    func perform() async throws -> some IntentResult {
        // Get vehicle
        guard let vehicleEntity = vehicle else {
            // Use primary vehicle if no vehicle specified
            let vehicles = VehicleService.shared.vehicles
            guard let primaryVehicle = vehicles.first(where: { $0.isPrimary }) ?? vehicles.first else {
                throw IntentError.noVehicle
            }
            
            return try await startWarmUp(for: primaryVehicle)
        }
        
        // Load vehicle from entity
        let vehicles = VehicleService.shared.vehicles
        guard let vehicle = vehicles.first(where: { $0.id == vehicleEntity.id }) else {
            throw IntentError.vehicleNotFound
        }
        
        return try await startWarmUp(for: vehicle)
    }
    
    @MainActor
    private func startWarmUp(for vehicle: Vehicle) async throws -> some IntentResult {
        // Get current temperature (复用 WeatherService)
        let weatherService = WeatherService.shared
        await weatherService.fetchCurrentTemperature()
        
        guard let temperature = weatherService.currentTemperature else {
            throw IntentError.temperatureUnavailable
        }
        
        // Calculate warm-up time
        let warmUpTime = WarmUpCalculationEngine.calculateWarmUpTime(
            temperature: temperature,
            vehicleType: vehicle.type,
            engineType: vehicle.engineType,
            hasBlockHeater: vehicle.hasBlockHeater,
            isPluggedIn: vehicle.isPluggedIn
        )
        
        // Start timer (复用 CountdownTimer)
        let timer = CountdownTimer()
        timer.start(
            minutes: warmUpTime,
            vehicleName: vehicle.name,
            vehicleId: vehicle.id,
            temperature: temperature
        )
        
        // Start Live Activity
        if #available(iOS 16.1, *) {
            LiveActivityManager.shared.startActivity(
                vehicleId: vehicle.id,
                vehicleName: vehicle.name,
                duration: warmUpTime * 60,
                temperature: temperature
            )
        }
        
        // Format response
        let tempDisplay = SettingsService.shared.settings.temperatureUnit.format(temperature)
        
        return .result(
            dialog: "Started warm-up timer for \(vehicle.name). " +
                   "Current temperature is \(tempDisplay). " +
                   "Recommended warm-up time is \(warmUpTime) minutes."
        )
    }
    
    static var parameterSummary: some ParameterSummary {
        Summary("Start warm-up for \(\.$vehicle)") {
            \.$vehicle
        }
    }
}

// MARK: - Intent Errors

enum IntentError: Error, CustomLocalizedStringResourceConvertible {
    case noVehicle
    case vehicleNotFound
    case temperatureUnavailable
    
    var localizedStringResource: LocalizedStringResource {
        switch self {
        case .noVehicle:
            return "No vehicles found. Please add a vehicle in the HotCar app first."
        case .vehicleNotFound:
            return "Vehicle not found."
        case .temperatureUnavailable:
            return "Unable to get current temperature. Please check your internet connection."
        }
    }
}
```

**CheckTemperatureIntent.swift**
```swift
//
//  CheckTemperatureIntent.swift
//  hotcarIntents
//
//  HotCar Siri Intent - Check Temperature
//

import AppIntents
import Foundation

@available(iOS 16.0, *)
struct CheckTemperatureIntent: AppIntent {
    
    static var title: LocalizedStringResource = "Check Temperature"
    static var description = IntentDescription(
        "Check current temperature and get warm-up recommendation"
    )
    
    static var openAppWhenRun: Bool = false
    
    @MainActor
    func perform() async throws -> some IntentResult {
        // Get current temperature (复用 WeatherService)
        let weatherService = WeatherService.shared
        await weatherService.fetchCurrentTemperature()
        
        guard let temperature = weatherService.currentTemperature else {
            throw IntentError.temperatureUnavailable
        }
        
        // Get primary vehicle (复用 VehicleService)
        let vehicles = VehicleService.shared.vehicles
        guard let vehicle = vehicles.first(where: { $0.isPrimary }) ?? vehicles.first else {
            throw IntentError.noVehicle
        }
        
        // Calculate warm-up time
        let warmUpTime = WarmUpCalculationEngine.calculateWarmUpTime(
            temperature: temperature,
            vehicleType: vehicle.type,
            engineType: vehicle.engineType,
            hasBlockHeater: vehicle.hasBlockHeater,
            isPluggedIn: vehicle.isPluggedIn
        )
        
        // Format response (复用 SettingsService)
        let settings = SettingsService.shared.settings
        let tempDisplay = settings.temperatureUnit.format(temperature)
        let locationName = LocationService.shared.locationName
        
        return .result(
            dialog: "Current temperature in \(locationName) is \(tempDisplay). " +
                   "Recommended warm-up time for your \(vehicle.name) is \(warmUpTime) minutes."
        )
    }
}

// MARK: - App Shortcuts Provider

@available(iOS 16.0, *)
struct HotCarShortcuts: AppShortcutsProvider {
    
    static var appShortcuts: [AppShortcut] {
        AppShortcut(
            intent: StartWarmUpIntent(),
            phrases: [
                "Start warm-up with \(.applicationName)",
                "Warm up my car with \(.applicationName)",
                "Start engine warm-up with \(.applicationName)"
            ],
            shortTitle: "Start Warm-Up",
            systemImageName: "flame.fill"
        )
        
        AppShortcut(
            intent: CheckTemperatureIntent(),
            phrases: [
                "Check temperature with \(.applicationName)",
                "What's the temperature with \(.applicationName)",
                "Check weather with \(.applicationName)"
            ],
            shortTitle: "Check Temperature",
            systemImageName: "thermometer"
        )
    }
}
```

#### ✅ 验证标准

##### 功能验证
- [ ] Siri 识别快捷指令
- [ ] 语音命令正常工作
- [ ] 无车辆时提示正确
- [ ] 温度获取失败时提示正确
- [ ] 计时器正常启动

##### 代码复用验证
- [ ] 复用 VehicleService.shared
- [ ] 复用 WeatherService.shared
- [ ] 复用 SettingsService.shared
- [ ] 复用 LocationService.shared
- [ ] 复用 CountdownTimer

---

### 优化项 6: 社交分享功能

#### 📝 需求描述
添加社交分享功能，允许用户分享燃油节省和环保贡献。

#### 🔍 现有代码分析

**已有模块**:
- ✅ `StatisticsService` - 已存在，可复用
- ✅ `WarmUpSession` - 已存在，可复用

**需要新增**:
- Share 模块
- ShareView

#### 🎯 生成规则

##### 1. 文件结构
```
Features/
├── Sharing/
│   ├── ShareService.swift (新增)
│   ├── ShareView.swift (新增)
│   └── ShareData.swift (新增)
```

##### 2. 代码编写示例

**ShareData.swift**
```swift
//
//  ShareData.swift
//  hotcar
//
//  HotCar Sharing - Share Data Model
//

import Foundation

struct ShareData {
    let fuelSaved: Double // in liters
    let moneySaved: Double // in dollars
    let co2Reduced: Double // in kg
    let sessions: Int
    let period: SharePeriod
    
    enum SharePeriod {
        case weekly
        case monthly
        case allTime
        
        var displayName: String {
            switch self {
            case .weekly: return "This Week"
            case .monthly: return "This Month"
            case .allTime: return "All Time"
            }
        }
    }
    
    var formattedFuelSaved: String {
        String(format: "%.1f L", fuelSaved)
    }
    
    var formattedMoneySaved: String {
        String(format: "$%.2f", moneySaved)
    }
    
    var formattedCO2Reduced: String {
        String(format: "%.1f kg", co2Reduced)
    }
    
    var shareText: String {
        """
        🚗 My HotCar Stats (\(period.displayName)):
        
        💰 Money Saved: \(formattedMoneySaved)
        ⛽ Fuel Saved: \(formattedFuelSaved)
        🌱 CO₂ Reduced: \(formattedCO2Reduced)
        🔥 Warm-Up Sessions: \(sessions)
        
        Download HotCar to optimize your winter warm-ups!
        #HotCar #WinterWarmUp #FuelEfficiency
        """
    }
}
```

**ShareService.swift**
```swift
//
//  ShareService.swift
//  hotcar
//
//  HotCar Service - Social Sharing
//  Generates shareable content from statistics
//

import Foundation
import UIKit

final class ShareService {
    
    // MARK: - Singleton
    
    static let shared = ShareService()
    
    // MARK: - Dependencies (复用现有服务)
    
    private let statisticsService = StatisticsService.shared // 已存在
    
    // MARK: - Initialization
    
    private init() {}
    
    // MARK: - Public Methods
    
    func generateShareData(for period: ShareData.SharePeriod) -> ShareData {
        let sessions = getSessions(for: period)
        
        let fuelSaved = calculateFuelSaved(sessions: sessions)
        let moneySaved = calculateMoneySaved(fuelSaved: fuelSaved)
        let co2Reduced = calculateCO2Reduced(fuelSaved: fuelSaved)
        
        return ShareData(
            fuelSaved: fuelSaved,
            moneySaved: moneySaved,
            co2Reduced: co2Reduced,
            sessions: sessions.count,
            period: period
        )
    }
    
    func generateShareImage(from data: ShareData) -> UIImage? {
        // TODO: Generate shareable image with statistics
        return nil
    }
    
    // MARK: - Private Methods
    
    private func getSessions(for period: ShareData.SharePeriod) -> [WarmUpSession] {
        let calendar = Calendar.current
        let now = Date()
        
        switch period {
        case .weekly:
            let weekAgo = calendar.date(byAdding: .weekOfYear, value: -1, to: now)!
            return statisticsService.getSessions(from: weekAgo)
            
        case .monthly:
            let monthAgo = calendar.date(byAdding: .month, value: -1, to: now)!
            return statisticsService.getSessions(from: monthAgo)
            
        case .allTime:
            return statisticsService.getAllSessions()
        }
    }
    
    private func calculateFuelSaved(sessions: [WarmUpSession]) -> Double {
        // Calculate fuel saved by optimal warm-up times
        // Assume optimal warm-up is 10 minutes
        let optimalMinutes = 10.0
        
        return sessions.reduce(0.0) { total, session in
            let actualMinutes = Double(session.durationMinutes)
            let wastedMinutes = max(0, actualMinutes - optimalMinutes)
            
            // Assume 0.5 L/hour at idle
            let fuelWasted = wastedMinutes * 0.5 / 60.0
            
            return total + fuelWasted
        }
    }
    
    private func calculateMoneySaved(fuelSaved: Double) -> Double {
        // Average gas price: $1.60/L
        return fuelSaved * 1.60
    }
    
    private func calculateCO2Reduced(fuelSaved: Double) -> Double {
        // 1 liter of gasoline produces ~2.3 kg CO2
        return fuelSaved * 2.3
    }
}
```

**ShareView.swift**
```swift
//
//  ShareView.swift
//  hotcar
//
//  HotCar UI - Share Statistics View
//

import SwiftUI

struct ShareView: View {
    
    // MARK: - Properties
    
    @State private var selectedPeriod: ShareData.SharePeriod = .weekly
    @State private var shareData: ShareData?
    
    // MARK: - Dependencies (复用现有服务)
    
    private let shareService = ShareService.shared
    
    // MARK: - Body
    
    var body: some View {
        VStack(spacing: 20) {
            // Period picker
            Picker("Period", selection: $selectedPeriod) {
                Text("This Week").tag(ShareData.SharePeriod.weekly)
                Text("This Month").tag(ShareData.SharePeriod.monthly)
                Text("All Time").tag(ShareData.SharePeriod.allTime)
            }
            .pickerStyle(.segmented)
            .padding()
            
            // Statistics cards
            if let data = shareData {
                VStack(spacing: 16) {
                    StatCard(
                        icon: "dollarsign.circle.fill",
                        title: "Money Saved",
                        value: data.formattedMoneySaved,
                        color: .hotCarPrimary
                    )
                    
                    StatCard(
                        icon: "fuelpump.fill",
                        title: "Fuel Saved",
                        value: data.formattedFuelSaved,
                        color: .hotCarSecondary
                    )
                    
                    StatCard(
                        icon: "leaf.fill",
                        title: "CO₂ Reduced",
                        value: data.formattedCO2Reduced,
                        color: .hotCarAccent
                    )
                    
                    StatCard(
                        icon: "flame.fill",
                        title: "Warm-Up Sessions",
                        value: "\(data.sessions)",
                        color: .hotCarWarning
                    )
                }
                .padding()
                
                // Share button
                ShareLink(item: data.shareText) {
                    HStack {
                        Image(systemName: "square.and.arrow.up")
                        Text("Share Stats")
                    }
                    .font(.headline)
                    .foregroundColor(.white)
                    .frame(maxWidth: .infinity)
                    .padding()
                    .background(Color.hotCarPrimary)
                    .cornerRadius(12)
                }
                .padding()
            }
        }
        .background(Color.backgroundPrimary)
        .onAppear {
            shareData = shareService.generateShareData(for: selectedPeriod)
        }
        .onChange(of: selectedPeriod) { newPeriod in
            shareData = shareService.generateShareData(for: newPeriod)
        }
    }
}

// MARK: - Stat Card

struct StatCard: View {
    let icon: String
    let title: String
    let value: String
    let color: Color
    
    var body: some View {
        HStack {
            Image(systemName: icon)
                .font(.title2)
                .foregroundColor(color)
                .frame(width: 40)
            
            VStack(alignment: .leading, spacing: 4) {
                Text(title)
                    .font(.caption)
                    .foregroundColor(.textSecondary)
                
                Text(value)
                    .font(.title2)
                    .fontWeight(.bold)
                    .foregroundColor(.textPrimary)
            }
            
            Spacer()
        }
        .padding()
        .background(Color.backgroundSecondary)
        .cornerRadius(12)
    }
}
```

#### ✅ 验证标准

##### 功能验证
- [ ] 分享数据正确生成
- [ ] 分享文本格式正确
- [ ] 分享功能正常工作
- [ ] 统计数据准确

##### 代码复用验证
- [ ] 复用 StatisticsService.shared
- [ ] 复用 WarmUpSession
- [ ] 复用 getSessions 方法

---

## 🟢 低优先级优化项

### 优化项 7: AI 学习功能（概要）

#### 📝 需求描述
添加机器学习功能，根据用户习惯优化预热时间建议。

#### 🎯 实施概要

**需要新增**:
- ML 模块
- CoreML 集成
- 用户行为分析

**实施步骤**:
1. 收集用户行为数据
2. 训练 ML 模型
3. 集成到 WarmUpCalculationEngine
4. 提供个性化建议

---

### 优化项 8: 多语言支持（概要）

#### 📝 需求描述
添加多语言支持，支持英语、法语、西班牙语等。

#### 🎯 实施概要

**需要新增**:
- Localization 模块
- Localizable.strings 文件
- 语言切换功能

**实施步骤**:
1. 提取所有用户可见文本
2. 创建 Localizable.strings 文件
3. 添加语言切换功能
4. 测试所有语言

---

## 📅 实施时间表

### 第一周：高优先级优化（4小时）

| 日期 | 任务 | 预计时间 |
|------|------|---------|
| Day 1 | 集成 LocationService 到 WeatherService | 1小时 |
| Day 2-3 | 增强 CountdownTimer | 2小时 |
| Day 4 | 添加地区检测到 AppSettings | 1小时 |

### 第二周：中优先级优化（9小时）

| 日期 | 任务 | 预计时间 |
|------|------|---------|
| Day 1-2 | Live Activity 支持 | 4小时 |
| Day 3-4 | Siri 快捷指令 | 3小时 |
| Day 5 | 社交分享功能 | 2小时 |

### 第三周：低优先级优化（可选）

| 日期 | 任务 | 预计时间 |
|------|------|---------|
| Day 1-3 | AI 学习功能 | 6小时 |
| Day 4-5 | 多语言支持 | 4小时 |

---

## ✅ 验证标准

### 代码质量标准

- [ ] 单元测试覆盖率 > 80%
- [ ] 无编译警告
- [ ] 无内存泄漏
- [ ] 代码符合 SwiftLint 规范
- [ ] 所有新代码有文档注释

### 功能验证标准

- [ ] 所有功能按需求实现
- [ ] 边界条件处理正确
- [ ] 错误处理完善
- [ ] 用户体验流畅

### 性能验证标准

- [ ] 应用启动时间 < 2秒
- [ ] API 响应时间 < 3秒
- [ ] 内存占用 < 50MB
- [ ] CPU 占用 < 10%

### 兼容性验证标准

- [ ] iOS 17.0+ 正常运行
- [ ] iPhone 和 iPad 均正常
- [ ] 模拟器和真机均正常
- [ ] 不同屏幕尺寸适配正确

### 代码复用验证标准

- [ ] 复用所有已存在的服务
- [ ] 不重复创建已存在的模块
- [ ] 保持现有架构一致性
- [ ] 不破坏现有功能

---

## 📝 实施注意事项

### Git 分支策略

```bash
# 创建功能分支
git checkout -b feature/location-integration
git checkout -b feature/timer-enhancement
git checkout -b feature/region-detection
git checkout -b feature/live-activity
git checkout -b feature/siri-intents
git checkout -b feature/social-sharing

# 合并到主分支前进行代码审查
git checkout main
git merge --no-ff feature/location-integration
```

### 提交信息规范

```
feat: 集成 LocationService 到 WeatherService

- 移除硬编码坐标
- 使用 LocationService 获取真实位置
- 添加位置缓存支持

Closes #1
```

### 测试策略

1. **单元测试**: 每个新功能都有对应的单元测试
2. **集成测试**: 测试模块间的交互
3. **UI 测试**: 测试用户界面流程
4. **性能测试**: 测试性能指标

### 发布流程

1. 完成 code review
2. 运行所有测试
3. 更新版本号
4. 创建 release branch
5. 提交到 App Store

---

## 🎯 总结

本改进计划已根据现有代码库进行全面修订，确保：

1. ✅ **最大化代码复用** - 复用所有已存在的服务和模块
2. ✅ **最小化改动** - 只修改必要的部分，不重复造轮子
3. ✅ **保持架构一致** - 遵循现有的设计模式和代码风格
4. ✅ **向后兼容** - 不破坏现有功能，保持向后兼容性
5. ✅ **可测试性** - 每个优化项都有对应的测试用例

### 关键改进点

1. **位置服务集成** - 移除硬编码，使用真实位置
2. **计时器增强** - 添加状态持久化和 Widget 集成
3. **地区检测** - 美国市场默认华氏度
4. **Live Activity** - Dynamic Island 支持
5. **Siri 集成** - 语音控制预热
6. **社交分享** - 分享节能成果

### 预期效果

- 🚀 **性能提升**: 优化位置获取和天气更新
- 🎯 **用户体验**: 更智能的温度单位和时间建议
- 📱 **功能完善**: Live Activity 和 Siri 快捷指令
- 🌍 **市场适配**: 美国市场本地化优化
- 🔧 **代码质量**: 更好的架构和测试覆盖率
